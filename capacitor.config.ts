import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SecurityMobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
